

// Aqui estarão todas as regras e proibições para o funcionamento do sistema.



// REGRAS PARA O CRUD

1. Sempre que for criar um registro no banco de dados com o UUID do usuário, sempre deve-se pegar o UUID do usuário que está logado no sistema. Evite pegar qualquer outra UUID de outras tabelas. Evite pegar a UUID da tabela colaboradores.

2. Evite editar outros arquivos que não foram solicitados.

3. EVITE Editar e alteraro o a página Schema.ts. ATENÇÃO! VOCÊ ESTA PROIBIDO DE MEXER NA PÁGINA SCHEMA.TS.